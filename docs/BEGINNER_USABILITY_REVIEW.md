# 初学者体验走查记录

本文持续记录以“第一次接触知识图谱、不了解资料处理与 AI 工作方式”的用户视角完成真实任务时遇到的问题。它不是立即开发清单；每条问题先保留证据和影响，再单独决定是否修改。

## 记录原则

- 使用打包后的 Windows 应用，而不是只看组件或单元测试。
- 默认使用全新临时资料库，不读取或修改日常用户数据。
- 真实书籍仅位于 `test-data/private/`，正文、截图和会话明细不提交 Git。
- 到云端发送前必须再次核对具体资料和范围；API Key 不写入本文件、日志或测试代码。
- 区分功能故障、可用性摩擦和未来增强，避免把所有观察都当成必须立刻修改的缺陷。

## 2026-09-23：两本真实 PDF，从首次启动到 AI 发送确认

### 测试资料与结果

| 资料 | 大小 | 页面 | 提取字符 | 元数据 | 结果 |
| --- | ---: | ---: | ---: | --- | --- |
| 《深入理解 AI Agent：设计原理与工程实践》 | 10.6 MB | 310 | 501,722 | 标题、作者正确 | 成功预览并导入 |
| 《深入理解 AI Infra：量化分析与系统设计》 | 7.6 MB | 518 | 695,784 | 标题、作者正确 | 成功预览并导入 |

- 同时选择两本书后，约 4.0 秒出现第一本可操作预览。
- 第一本确认保存约 0.2 秒，随后自动进入第二本预览；第二本保存约 0.15 秒。
- 两本书均能重建正文，未产生阻断警告。界面明确提醒复杂分栏、脚注和表格仍需人工核对。
- AI Infra 平均每页约 1,343 字符，3 页少于 200 字符；搜索“Transformer”命中 20 页。
- AI Agent 平均每页约 1,618 字符，4 页少于 200 字符；搜索“Agent”达到 50 条显示上限。
- 成功创建目标图谱并到达 DeepSeek 最终发送确认页；确认框未勾选时发送按钮不可用。
- 本轮使用临时占位设置验证界面，没有向 DeepSeek 发送正文。真实生成仍待用户在日常应用中安全配置 Key 后单独验收。

### 初学者实际旅程

1. 欢迎页选择“导入本地资料”。页面能解释资料先在本机预览，并且不要求立即配置 DeepSeek。
2. 在资料库同时选择两本 PDF。解析期间按钮显示“正在解析所选资料…”，约 4 秒后进入第 1 / 2 本预览。
3. 预览页显示文件大小、页数、字符数、可编辑书目信息、前 12 页正文和提取说明。确认第一本后进入第 2 / 2 本。
4. 确认第二本后直接停留在第二本的正文阅读页。返回资料库后才能看到两本书均已导入。
5. 正文阅读器支持搜索、页码跳转和按页目录；搜索“Transformer”能得到 20 个匹配页面。
6. 点击“AI 生成学习路线”后，由于尚未配置密钥，界面直接打开 AI 设置。
7. 配置状态满足后再次点击，进入目标图谱与连续范围选择。没有图谱时可在当前对话框内创建。
8. 默认范围是当前第 1 页：仅 51 字符，但界面仍认为可生成，并展示为 1 节、1 批。
9. 下一步能看到目标图谱、资料、模型、具体页、字符数、批次数和正文摘录；必须勾选同意后才能发送。

## 观察清单

| 编号 | 优先级 | 观察 | 对初学者的影响 | 建议方向 | 状态 |
| --- | --- | --- | --- | --- | --- |
| UX-001 | 高 | PDF 只形成“第 N 页”，两本书都没有语义章节；AI Infra 的范围下拉框包含 518 个同质选项。 | 用户不了解书的结构，也很难判断应该选择哪些连续页面。 | 尝试读取 PDF 书签/目录，或在本地识别章节标题；至少为范围选择增加搜索、页码直达和正文摘要。 | 已记录 |
| UX-002 | 高 | 打开资料默认位于封面页，AI Infra 第 1 页仅 51 字符，仍可直接进入发送确认。 | 用户可能花费 API 调用生成一个几乎没有学习价值的路线，并误以为 AI 效果差。 | 对过短、封面、版权页和目录页给出明显提醒；推荐从正文密集页或搜索结果开始，避免武断禁止。 | 已记录 |
| UX-003 | 高 | 真实 DeepSeek 请求尚未完成端到端验收。 | 当前只能证明上传前边界可靠，不能证明真实响应质量、耗时、取消、失败重试和候选审核负担。 | 使用用户自行保存的 Key，明确选择一小段正文后完成一次真实调用并记录质量。 | 等待真实验收 |
| UX-004 | 中 | 点击 AI 功能后才突然进入 API Key 设置。设置页说明了安全存储，但没有说明去哪里获取 Key、是否需要余额、配置后会回到哪里。 | 完全不了解 API 的用户可能在这里中断，并怀疑还缺少其他软件。 | 在设置页增加简短的“获取与费用说明”链接，以及保存成功后的明确返回路径。 | 已记录 |
| UX-005 | 中 | 批量导入全部完成后直接打开最后一本正文，没有批次完成摘要。 | 用户可能不确定第一本是否也成功保存，需要手动返回资料库确认。 | 最后一本保存后显示“2 本已导入”的短暂完成状态，并提供“开始阅读 / 返回资料库”两个入口。 | 已记录 |
| UX-006 | 中 | 提取说明要求用户核对分栏、脚注、表格阅读顺序，但没有告诉用户该看什么、抽查哪几页。 | 新手会直接忽略提醒，或因不知道如何判断而产生负担。 | 提供三项简短检查清单，并推荐抽查首页、正文密集页和表格页。 | 已记录 |
| UX-007 | 中 | 正文目录初次只显示 50 / 518 页，需要反复加载；搜索和页码跳转能缓解，但“章节”与“页面”术语混用。 | 用户可能把第 518 页理解成第 518 章，也不易建立内容全貌。 | PDF 明确使用“页面”，EPUB/Markdown 使用“章节”；在长目录中增加当前位置和快速定位。 | 已记录 |
| UX-008 | 低 | 资料列表信息清楚，但没有“从这本书创建学习路线”的直接入口。 | 用户必须先打开书，再在正文页发现 AI 操作。 | 在资料卡提供次级入口，同时仍要求进入范围确认，不绕过隐私步骤。 | 已记录 |

## 当前表现较好的细节

- 欢迎页把本地导入、手工建图和示例体验区分清楚，没有在首次启动索要 API Key。
- 多文件选择后按 1 / 2、2 / 2 逐本预览，单本确认不会丢失下一本。
- 提取速度、元数据识别和保存速度在这两本真实 PDF 上表现良好。
- 原文件不修改、哈希、上传时机与发送范围都写得明确。
- 目标图谱可以在 AI 范围对话框中创建，避免先退出流程。
- 最终发送确认页能明确展示资料、模型、页数、字符数、批次数和正文摘录，且默认不允许发送。

## 下一次走查

下一次应只选择正文中的一个短小、连续且有明确主题的范围，完成一次真实 DeepSeek 调用，重点记录：

- 等待期间是否知道当前在第几批、是否仍在工作；
- 取消后是否立即停止且不留下半成品；
- 网络或模型错误是否能安全重试；
- 概念数量、命名、描述、原文引用和先修关系质量；
- 初学者审核候选需要多少判断，以及哪些检查应由应用自动承担；
- 写入图谱后是否能自然进入第一次学习，而不是再次迷失在功能入口中。
